doctype html
html
  head
    meta(charset="utf-8")
    each file in lessFiles
      link(href='/css/' + file.target media=media rel="stylesheet" type="text/css")
    title Prototype
  body
    #main
    each dependency in externalDependencies
      script(src='/js/' + dependency)
    script(src='/js/' + jsOutFile)
