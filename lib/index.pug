doctype html
html
  head
    meta(charset="utf-8")
    link(href='/css/' + cssOutFile media="all" rel="stylesheet" type="text/css")
    title Prototype
  body
    #main
    each dependency in externalDependencies
      script(src='/js/' + dependency)
    script(src='/js/' + jsOutFile)
